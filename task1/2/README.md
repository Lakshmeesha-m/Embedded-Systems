Task 2: LED Control Using Push Button

Expected Output:
The LED turns ON when the push button is pressed and turns OFF when the button is released.

Methodology:
The push button is configured as an input and the LED as an output. The button state is continuously read using digitalRead(), and the LED state is updated accordingly.
