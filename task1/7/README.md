Task 7: IR Sensor Based LED Control

Expected Output:
The LED turns ON when an object is detected by the IR sensor and turns OFF when no object is present.

Methodology:
The IR sensor output is read using digitalRead(). Based on the sensor state, the LED is controlled accordingly.
