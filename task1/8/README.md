Task 8: IR Sensor Detection Count Blinking

Expected Output:
The LED blinks the same number of times as the number of objects detected by the IR sensor.

Methodology:
Each object detection increments a counter. The LED then blinks according to the counter value using a loop and delay.
